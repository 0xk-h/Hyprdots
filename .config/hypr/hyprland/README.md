# Hyprland Config Overrides (Hyprdots)

This folder contains only the small overrides I apply on top of the default Illogical Impulse's End-4 configs.  
Nothing here replaces the full config — these files only **patch specific sections**.

---

# 🔄 Reload

If changes doesn't apply, run

```bash

hyprctl reload

```
